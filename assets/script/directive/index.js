import popover from "./popover"
import tooltip from "./tooltip"
import loading from "./loading"
import ripple from "./ripple"
import scrollbar from "./scrollbar"

export default { popover, tooltip, loading, ripple, scrollbar }
